language: java
jdk:
  - oraclejdk8
sudo: required

before_install:
  - cd libdivide4j && mvn clean install -DskipTests && cd ../

script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - mvn test -DrunLongTests=true -B
